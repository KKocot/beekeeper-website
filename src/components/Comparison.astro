---
interface Row {
  feature: string;
  beekeeper: boolean | "na";
  python: boolean | "na" | "limited";
  manual: boolean | "na";
}

const rows: Row[] = [
  { feature: "Encrypted wallet storage", beekeeper: true, python: true, manual: false },
  { feature: "Auto-lock on inactivity", beekeeper: true, python: true, manual: false },
  { feature: "Multi-session support", beekeeper: true, python: true, manual: false },
  { feature: "HTTP/WebSocket API", beekeeper: true, python: true, manual: false },
  { feature: "Browser WASM support", beekeeper: true, python: false, manual: false },
  { feature: "Transaction signing", beekeeper: true, python: true, manual: true },
  { feature: "Key import/export", beekeeper: true, python: true, manual: true },
  { feature: "Memo encryption", beekeeper: true, python: true, manual: false },
  { feature: "Bundle < 274 KB", beekeeper: true, python: "na", manual: "na" },
  { feature: "Embedded binary (zero config)", beekeeper: "na", python: true, manual: false },
  { feature: "Sync & Async API", beekeeper: true, python: true, manual: "na" },
  { feature: "Full type hints", beekeeper: "na", python: true, manual: false },
  { feature: "Cross-platform support", beekeeper: true, python: "limited", manual: true },
];
---

<section id="comparison" aria-labelledby="comparison-heading" class="section-reveal px-4 py-24">
  <div class="mx-auto max-w-4xl">
    {/* Section header */}
    <div class="mb-16 text-center">
      <h2 id="comparison-heading" class="text-3xl font-bold text-text-primary md:text-4xl">
        Stop managing keys manually
      </h2>
    </div>

    {/* Table wrapper — horizontal scroll on mobile */}
    <div
      class="comparison-table overflow-x-auto rounded-xl border border-border-subtle bg-bg-elevated"
    >
      <table class="w-full min-w-[36rem]">
        <thead>
          <tr class="bg-bg-subtle">
            <th
              scope="col"
              class="px-6 py-4 text-left text-sm font-medium uppercase tracking-wider text-text-secondary"
            >
              Feature
            </th>
            <th
              scope="col"
              class="px-6 py-4 text-center text-sm font-semibold uppercase tracking-wider text-accent"
            >
              Beekeeper
            </th>
            <th
              scope="col"
              class="px-6 py-4 text-center text-sm font-semibold uppercase tracking-wider text-success"
            >
              Python SDK
            </th>
            <th
              scope="col"
              class="px-6 py-4 text-center text-sm font-medium uppercase tracking-wider text-text-secondary"
            >
              Manual
            </th>
          </tr>
        </thead>
        <tbody>
          {
            rows.map((row, i) => (
              <tr
                class:list={[
                  "comparison-row",
                  i < rows.length - 1 ? "border-b border-border-subtle" : "",
                ]}
              >
                <td class="px-6 py-4 text-sm text-text-primary">
                  {row.feature}
                </td>
                <td class="px-6 py-4 text-center">
                  {row.beekeeper === "na" ? (
                    <span class="text-sm text-text-tertiary">N/A</span>
                  ) : row.beekeeper ? (
                    <span class="text-lg font-bold text-success" aria-label="Yes">
                      &#10003;
                    </span>
                  ) : (
                    <span class="text-lg font-bold text-error" aria-label="No">
                      &#10007;
                    </span>
                  )}
                </td>
                <td class="px-6 py-4 text-center">
                  {row.python === "na" ? (
                    <span class="text-sm text-text-tertiary">N/A</span>
                  ) : row.python === "limited" ? (
                    <span class="text-sm font-medium text-accent" aria-label="Limited">
                      Linux only
                    </span>
                  ) : row.python ? (
                    <span class="text-lg font-bold text-success" aria-label="Yes">
                      &#10003;
                    </span>
                  ) : (
                    <span class="text-lg font-bold text-error" aria-label="No">
                      &#10007;
                    </span>
                  )}
                </td>
                <td class="px-6 py-4 text-center">
                  {row.manual === "na" ? (
                    <span class="text-sm text-text-tertiary">N/A</span>
                  ) : row.manual ? (
                    <span
                      class="text-lg font-bold text-success"
                      aria-label="Yes"
                    >
                      &#10003;
                    </span>
                  ) : (
                    <span
                      class="text-lg font-bold text-error"
                      aria-label="No"
                    >
                      &#10007;
                    </span>
                  )}
                </td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </div>
</section>

<style>
  .comparison-table {
    opacity: 0;
    transform: translateY(1rem);
    transition:
      opacity 0.6s ease-out,
      transform 0.6s ease-out;
  }

  .comparison-table.visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .comparison-table {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<script>
  const table = document.querySelector(".comparison-table");
  const prefersReducedMotion = window.matchMedia(
    "(prefers-reduced-motion: reduce)"
  ).matches;

  if (prefersReducedMotion) {
    table?.classList.add("visible");
  } else if (table) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            e.target.classList.add("visible");
            observer.unobserve(e.target);
          }
        });
      },
      { threshold: 0.1 }
    );
    observer.observe(table);
  }
</script>
